node_modules/
dist/
coverage/
.vite/
*.log
playwright-report/
test-results/

# TypeScript composite-project emit (required by tsc -b, never used by anyone)
.tsbuild-node/
*.tsbuildinfo

# Build output (vite outDir → ../src/another_s3_manager/static/v2/) is ignored
# from the repo-root .gitignore — gitignore patterns can't escape this dir.
